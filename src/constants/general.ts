// Selectively copied from infinidraft's src/constants/general.ts - just the
// two constants PageContainer/AppHeader need for layout parity between the
// two apps. Keep these in sync with infinidraft's values if either changes.
export const APP_CONTENT_MAX_WIDTH = 1600;
export const MOBILE_HEADER_HEIGHT = 85;
